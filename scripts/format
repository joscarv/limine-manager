#!/usr/bin/env bash
set -euo pipefail
root=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)
mapfile -d '' files < <(find "$root/include" "$root/src" "$root/tests" -type f \( -name '*.cpp' -o -name '*.hpp' \) -print0)
((${#files[@]})) && clang-format -i "${files[@]}"
